# Regenerated by setup.sh — never commit (large / machine-specific)
venv/
opt/
vendor/
output/

# Static ffmpeg binaries land here; the bin/tesseract wrapper IS committed
bin/ffmpeg
bin/ffprobe

# Python
__pycache__/
*.pyc
